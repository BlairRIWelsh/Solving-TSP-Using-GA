% 
%
% ---------------------------------------------------------
function [temp_chromosome_1,temp_chromosome_2] = Mutation(temp_chromosome_1,temp_chromosome_2)
%% mutation prob 0.2 and random pick bit to switch
%         if (rand < 0.2)
%             % flip a random bit of temp_chromosome_1
%         end
%         if (rand < 0.2)
%             % flip a random bit of temp_chromosome_2 
%         end